# SUBMISSION — ZZALKAK Studio (짤칵 스튜디오)

## 결과물 주소

`(배포 후 이 자리에 실제 배포 URL을 입력하세요. 예: Vercel/Netlify/GitHub Pages 등 — 로그인·인증·초대·CAPTCHA 없이 시크릿 창에서 바로 열려야 합니다.)`

## 소스 주소

`(이 자리에 공개 저장소 URL을 입력하세요. 예: https://github.com/<사용자명>/<저장소명> — 시크릿 창에서 로그인 없이 코드가 보여야 합니다.)`

## 짧은 확인 방법

### 1. 이미지 또는 단색 배경 + 문구 실시간 편집

1. **어디로 가나요**: 배포 주소 접속 → 첫 화면 (왼쪽 텍스트 도구, 중앙 캔버스, 오른쪽 속성 패널이 이미 열려 있음)
2. **3단계 이내 무엇을 하나요**: 중앙 캔버스에서 "파일 선택"으로 PNG/JPEG를 올리거나 "단색 배경으로 시작" 클릭 → 오른쪽 속성 패널의 문구 내용에 글자 입력 → 글자 크기·색상·위치 슬라이더 아무거나 움직이기
3. **무엇이 보이면 통과인가요**: 중앙 캔버스에 이미지(또는 선택한 단색)가 나타나고, 슬라이더를 움직이는 즉시(별도 버튼 없이) 문구 위치·크기·색이 바뀐다
4. **안 될 때 무엇이 보이나요**: 캔버스가 빈 상태에 머물러 있거나, 슬라이더를 움직여도 문구가 그대로면 실패

### 2. 지원하지 않는 파일 거부

1. **어디로 가나요**: 이미지가 이미 불러와진 상태에서 왼쪽 도구 사이드바 "이미지"
2. **3단계 이내 무엇을 하나요**: "이미지 교체"에서 GIF·PDF·TXT 등 PNG/JPEG가 아닌 파일 선택
3. **무엇이 보이면 통과인가요**: "PNG와 JPEG 형식의 이미지만 지원합니다"를 포함한 오류 토스트가 뜨고, 기존에 불러온 이미지와 입력했던 문구가 그대로 남아 있다
4. **안 될 때 무엇이 보이나요**: 오류 문장이 없거나, 기존 이미지/문구가 사라지거나 초기화됨

### 3. 화면비별 다운로드 일치

1. **어디로 가나요**: 상단 앱 바의 화면비 선택(1:1 / 4:5 / 9:16)과 "PNG 다운로드" 버튼
2. **3단계 이내 무엇을 하나요**: 1:1 선택 후 다운로드 → 4:5 선택 후 다운로드 → 9:16 선택 후 다운로드
3. **무엇이 보이면 통과인가요**: 각 다운로드 파일을 열었을 때 이미지 잘림·문구 위치·줄바꿈·색상이 다운로드 직전 화면의 캔버스와 완전히 같다
4. **안 될 때 무엇이 보이나요**: 다운로드가 되지 않거나(오류 토스트 표시), 파일을 열었을 때 캔버스와 문구 위치·잘림이 다르게 보임

### 4. 템플릿 저장·불러오기·수정·삭제

1. **어디로 가나요**: 왼쪽 도구 사이드바 "템플릿" (또는 상단 앱 바 "템플릿 관리")
2. **3단계 이내 무엇을 하나요**: 이름 입력 후 "저장" 2~3회 → 하나 "불러오기" → 하나 "이름 변경" 또는 "삭제"
3. **무엇이 보이면 통과인가요**: 템플릿 목록에 저장한 개수만큼 나타나고, 불러오기·수정·삭제가 즉시 목록에 반영되며, 새로고침해도 그대로 남아 있다(단색 배경 템플릿은 색상까지 그대로 복원됨)
4. **안 될 때 무엇이 보이나요**: 새로고침 후 템플릿이 모두 사라지거나, 삭제/수정 대상이 클릭한 것과 다른 템플릿에 적용됨

### 5. 스티커 추가와 편집

1. **어디로 가나요**: 왼쪽 도구 사이드바 "스티커" (이미지 또는 단색 배경이 있어야 함)
2. **3단계 이내 무엇을 하나요**: 썸네일 하나 클릭 → 캔버스에서 드래그해 옮기고 모서리 손잡이로 크기·회전 조절 → `Backspace` 또는 `Delete`로 삭제
3. **무엇이 보이면 통과인가요**: 캔버스 가운데에 스티커가 생기고 파란 선택 테두리가 따라오며, 다운로드한 PNG에도 같은 위치·크기·회전으로 들어가 있다
4. **안 될 때 무엇이 보이나요**: 클릭해도 캔버스에 아무것도 안 생기거나, 다운로드 파일에서 스티커가 빠져 있음

### 6. 도구 검색 (Command Palette)

1. **어디로 가나요**: 상단 앱 바 "도구 또는 기능 검색" 버튼 또는 `Ctrl+K`(`⌘K`)
2. **3단계 이내 무엇을 하나요**: "9:16" 입력 → Enter
3. **무엇이 보이면 통과인가요**: 캔버스가 세로형(9:16)으로 즉시 바뀌고 상단 화면비 표시가 9:16으로 강조된다
4. **안 될 때 무엇이 보이나요**: 검색창이 열리지 않거나, Enter를 눌러도 화면비가 그대로임

### 7. JSON 내보내기/가져오기 (정상·손상·필드 누락)

1. **어디로 가나요**: 상단 앱 바 "가져오기/내보내기" (모달)
2. **3단계 이내 무엇을 하나요**: 먼저 내보내기로 JSON 파일 받기 → 그 파일을 다시 가져오기 → 이번엔 메모장에서 중괄호를 하나 지운 손상된 JSON을 가져오기
3. **무엇이 보이면 통과인가요**: 정상 파일은 "OO개 템플릿을 가져왔습니다" 메시지와 함께 목록이 늘어나고, 손상된 파일은 "JSON 문법이 올바르지 않습니다" 류의 오류만 뜨고 기존 목록 개수는 그대로다
4. **안 될 때 무엇이 보이나요**: 손상된 파일을 가져왔는데도 목록 개수가 바뀌거나, 오류 메시지 없이 조용히 실패함

## AI 사용 기록

### 1. AI에게 맡긴 일

- `CHECK.md`/`CLAUDE.md`에 정리된 요구사항을 분석해 Vite + React + TypeScript 프로젝트를 처음부터 구성하는 작업 전체.
- 이미지 업로드 검증, 실시간 문구 편집(제목/소제목/본문·글꼴·위치/크기/색/정렬/줄간격), 화면비별 `cover` 배치, 미리보기·다운로드 공통 렌더링 함수(`src/lib/render.ts`), 템플릿 CRUD, JSON 내보내기/가져오기 검증 로직 등 초기 기능 구현.
- UI를 카드형 세로 나열에서 애플리케이션형 3단 편집기(상단 앱 바·왼쪽 도구 사이드바·중앙 캔버스·오른쪽 속성 패널)로 전면 개편하고, 스티커 도구(프로젝트 내부 PNG 16종, 드래그·크기·회전·레이어), 도구 검색(Command Palette), 실행 취소/다시 실행, 초안 자동 저장을 추가 구현.
- 사진 없이도 단색 배경만으로 카드를 만들고 다운로드할 수 있도록 배경을 "이미지/단색" 두 모드로 확장(`CardSettings.background`, 템플릿 스키마 v4, 텍스트·스티커 모두 각 요소를 `Backspace`/`Delete`로 삭제 가능하도록 단축키 보강).
- 헤드리스 브라우저(Playwright)로 실제 앱을 직접 조작해 CHECK.md의 통과 기준(T03-C03~C32)과 신규 기능을 하나씩 검증하는 작업. 이 과정에서 실제로 발견하고 고친 결함들: 다운로드 버튼이 미리보기 캔버스 참조를 못 받던 문제, 긴 여러 줄 문구가 캔버스 밖으로 잘리던 문제, 특정 글꼴에서 한글 글자 외곽선이 깨지던 문제, UI 개편 후 문구 편집 도구가 첫 화면에서 클릭 없이는 보이지 않던 문제(T03-C03 회귀), 단색 배경 모드로 바꿔도 스티커 도구가 여전히 "이미지 필요"로 막혀 있던 문제.
- 자체 배경 샘플 3장과 완성 이미지 3장을 코드(Canvas)로 직접 생성하는 작업, 스티커 원본 시트를 요소별로 잘라 개별 에셋으로 만드는 작업과 `ASSET_LICENSES.md`/`TEST_REPORT.md` 작성·갱신.

### 2. 학생이 직접 판단한 일

- `CLAUDE.md` 요구사항 문서 자체의 설계: 첫 화면에 노출할 영역 목록, 템플릿에 저장할 최소 데이터 항목, JSON 가져오기를 "파일 읽기 → 파싱 → 전체 검증 → 반영"의 트랜잭션 순서로 못박은 것, 12개 극단 입력 카테고리 선정, 완성 이미지 3개와 라이선스 문서화 요구, 짧은 확인 방법·AI 사용 기록을 구분해서 남기라는 문서화 규칙 — 이 모든 설계 판단은 학생이 사전에 작성한 요구사항 문서에 담겨 있었고, AI는 그 설계를 그대로 구현했다.
- UI 전면 개편의 방향(상단 앱 바/사이드바/캔버스/속성 패널 3단 구조, Canva·Figma·Linear를 참고하되 그대로 베끼지 않을 것), 스티커 기능의 세부 요구(검색·카테고리 필터·레이어 순서·비율 좌표 저장), "사진 없이도 카드를 만들 수 있어야 한다"는 이번 개선 요청 자체가 학생이 실제 사용 흐름을 검토하다 직접 발견하고 지시한 내용이다.
- CHECK.md 위반 여부를 AI에게 별도로 점검시키고, 그 결과 나온 T03-C03 회귀를 실제로 수정할지·어떤 방식으로 수정할지(첫 화면 기본 도구를 텍스트로 전환)를 학생이 선택했다.
- (제출 전 필수) 학생 본인이 배포 후 시크릿 창에서 로그인 없이 접속되는지, 위 "짧은 확인 방법" 7가지 시나리오가 실제로 통과하는지, `ASSET_LICENSES.md`의 스티커 출처 표(현재 "제출자가 기입" 상태)를 채워 T03-C27을 만족하는지 최종 검수해야 한다.

### 3. AI 제안을 따르지 않은 일

- 없음.
- **이유**: 이번 작업은 학생이 먼저 `CLAUDE.md`에 상세한 요구사항을 확정해 두고, 이후 UI 개편·스티커·단색 배경 등은 학생이 사용해 보며 필요한 개선을 그때그때 구체적으로 지시(예: "왼쪽 도구 사이드바에 스티커 도구를 추가해줘", "사진 없이도 카드를 만들 수 있도록 해줘")하는 방식으로 진행되었다. AI가 먼저 제시한 기술적 선택지를 학생이 검토 후 거부한 사례는 없었다. 다만 학생이 최종 코드 검수 단계에서 실제로 되돌리거나 다르게 바꾼 부분이 생기면, 제출 전 이 항목을 사실대로 갱신해야 한다.
