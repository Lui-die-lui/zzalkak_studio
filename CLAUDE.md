
프로젝트명은 **ZZALKAK Studio (짤칵 스튜디오)**이며, 사용자가 이미지를 불러와 한글 문구를 배치하고 PNG로 저장하며, 편집 설정을 템플릿으로 관리하는 공개 웹 도구다.

중요한 작업 원칙:

1. 기존 프로젝트의 프레임워크와 구조를 먼저 확인하고 그에 맞춰 구현한다.
2. 불필요한 서버·로그인·DB는 추가하지 않는다.
3. 템플릿 데이터는 브라우저 `localStorage`에 저장해 새로고침 후에도 유지한다.
4. 사용자가 선택한 이미지는 외부 서버로 전송하지 않고 브라우저 안에서만 처리한다.
5. 미리보기와 다운로드 결과는 반드시 동일한 렌더링 함수를 사용한다.
6. 지원하지 않는 파일이나 잘못된 JSON을 입력해도 현재 이미지, 편집값, 템플릿 목록을 절대 먼저 초기화하지 않는다.
7. 기능을 임의로 축소하지 말고 아래 통과 기준 전체를 구현한다.
8. 구현 완료 후 빌드와 주요 동작을 직접 확인하고 오류가 있으면 수정한다.

## 1. 첫 화면 구성

첫 화면에서 스크롤하지 않아도 다음 영역의 존재를 확인할 수 있게 구성한다.

* 이미지 불러오기 영역
* 문구 입력
* 문구 X/Y 위치 조절
* 문구 크기 조절
* 문구 색상 선택
* 화면비 선택
* 실시간 미리보기
* 이미지 다운로드 버튼
* 템플릿 관리 영역으로 이동하거나 확인할 수 있는 UI

반응형으로 구성하되 데스크톱에서는 편집 도구와 미리보기를 나란히 보여준다.

## 2. 이미지 불러오기와 안전한 오류 처리

다음 형식을 허용한다.

* PNG: `image/png`
* JPEG: `image/jpeg`

파일 확장자만 믿지 말고 MIME 타입도 검사한다.

지원하지 않는 GIF, SVG, PDF, TXT 등의 파일을 넣으면:

* 파일을 불러오지 않는다.
* 거부 이유를 사용자에게 명확히 표시한다.
* 현재 작업 중인 이미지와 문구, 위치, 크기, 색, 비율을 그대로 유지한다.
* 오류 메시지에는 허용 형식이 PNG와 JPEG라는 내용이 포함되어야 한다.

파일 크기 제한을 합리적으로 설정하고, 너무 큰 파일도 현재 작업을 유지한 채 거부한다.

업로드한 이미지는 서버로 보내지 않고 `FileReader` 또는 Object URL을 이용해 브라우저 안에서 처리한다.

Canvas에 다시 그린 뒤 다운로드하도록 구현하여 원본 이미지의 EXIF/GPS 등 위치 정보 메타데이터가 결과 파일에 복사되지 않게 한다.

## 3. 실시간 문구 편집

문구는 다음 항목을 편집할 수 있어야 한다.

* 문구 내용
* X 위치
* Y 위치
* 글자 크기
* 글자 색상

입력값을 바꾸면 별도의 적용 버튼 없이 미리보기에 즉시 반영한다.

X/Y 좌표는 출력 이미지의 실제 픽셀 좌표가 아니라 0~100%의 비율 좌표로 저장해 화면 크기가 달라져도 같은 위치에 나타나게 한다.

필요하면 드래그로 문구 위치를 바꾸는 기능도 추가하되, 범위를 벗어나 문구가 완전히 사라지지 않도록 제한한다.

긴 문장은 지정된 최대 폭 안에서 자동 줄바꿈한다. 사용자가 직접 입력한 줄바꿈도 유지한다.

다음을 안전하게 처리한다.

* 긴 한글
* 한글과 영문 혼합
* 숫자와 특수문자
* 이모지
* 여러 줄 문구
* 빈 문구

빈 문구는 오류가 아니라 이미지에 글자가 표시되지 않는 정상 상태로 처리한다.

한글을 안정적으로 표시할 수 있는 글꼴 스택을 사용한다. 웹폰트를 사용한다면 다운로드 전에 `document.fonts.ready`를 기다려 미리보기와 저장 파일의 글꼴 차이를 방지한다.

## 4. 화면비와 미리보기/다운로드 일치

다음 화면비를 제공한다.

* 1:1
* 4:5
* 9:16

화면비별 출력 크기는 코드에서 상수로 관리한다. 예시는 다음과 같다.

* 1:1 → 1080×1080
* 4:5 → 1080×1350
* 9:16 → 1080×1920

이미지는 선택한 비율을 채우도록 `cover` 방식으로 배치한다.

미리보기와 다운로드 파일은 다음이 일치해야 한다.

* 이미지 확대 비율
* 이미지 잘림 영역
* 문구 X/Y 위치
* 문구 크기 비율
* 문구 줄바꿈
* 문구 색상
* 줄 간격

미리보기용 CSS와 다운로드용 Canvas에서 서로 다른 계산을 중복 작성하지 않는다. 하나의 공통 레이아웃 계산 함수와 렌더링 기준을 사용한다.

화면 크기가 달라도 내부 기준 좌표는 선택한 출력 크기를 기준으로 유지하고, 미리보기에서만 동일 비율로 축소 표시한다.

다운로드 파일명에는 비율과 날짜 또는 템플릿 이름을 안전한 형식으로 포함한다.

## 5. 템플릿 CRUD

현재 편집 상태를 템플릿으로 저장할 수 있게 한다.

템플릿 데이터에는 최소한 다음 항목을 포함한다.

* 안정적인 고유 ID
* 템플릿 이름
* 문구
* X 위치
* Y 위치
* 글자 크기
* 글자 색상
* 화면비
* 생성 시각
* 수정 시각

배열 인덱스를 ID로 사용하지 말고 `crypto.randomUUID()` 등 안정적인 ID를 사용한다.

다음 기능을 모두 제공한다.

* 템플릿 생성
* 저장된 템플릿 불러오기
* 템플릿 이름 또는 설정 수정
* 템플릿 삭제
* 새로고침 후 유지

최소 3개의 템플릿을 사용자가 만들 수 있어야 하며, 개수 제한을 3개로 고정하지 않는다.

삭제 전에는 확인 절차를 제공한다. 현재 편집 중인 템플릿이 삭제되더라도 다른 템플릿이 잘못 수정되거나 삭제되지 않게 한다.

이미지 원본의 Base64 전체를 템플릿에 저장해 localStorage 용량을 낭비하지 않는다. 템플릿에는 편집 설정을 저장하고, 이미지가 필요하면 다시 선택하도록 명확히 안내한다.

## 6. JSON 내보내기와 가져오기

템플릿 전체를 JSON 파일로 내보낼 수 있게 한다.

JSON 가져오기는 다음 3가지 경우를 구분한다.

### 정상 JSON

* 스키마 검증 후 템플릿을 복원한다.
* 같은 ID가 있으면 충돌 처리 규칙을 명확히 적용한다.
* 정상 복원 결과와 가져온 개수를 표시한다.

### 문법이 손상된 JSON

* `JSON.parse` 단계에서 거부한다.
* 사용자가 이해할 수 있는 오류 메시지를 표시한다.
* 기존 템플릿 목록은 변경하지 않는다.

### 필수 항목이 빠진 JSON

* 모든 데이터를 먼저 검증한 후 거부한다.
* 일부 항목만 먼저 저장하지 않는다.
* 기존 템플릿 목록은 변경하지 않는다.
* 어떤 필수 항목이 잘못되었는지 표시한다.

가져오기 로직은 반드시 다음 순서로 처리한다.

1. 파일 읽기
2. JSON 파싱
3. 전체 데이터 구조와 필수 항목 검증
4. 모든 검증 통과
5. localStorage 반영
6. UI 다시 그리기

오류가 나면 5단계로 넘어가지 않는 트랜잭션 방식으로 구현한다.

## 7. 극단 입력 12건 검사

프로젝트에 `TEST_REPORT.md` 파일을 만들고 다음 12건 이상의 검사 기록을 표로 남긴다.

각 행에는 다음 열이 있어야 한다.

* 검사 ID
* 검사 입력
* 예상 결과
* 실제 결과
* PASS/FAIL
* 확인 방법
* 관련 통과 기준

반드시 포함할 입력:

1. 긴 한글 문장
2. 한글·영문·숫자 혼합
3. 사용자가 입력한 여러 줄 문구
4. 이모지 포함 문구
5. 빈 문구
6. 세로형 이미지
7. 가로형 이미지
8. 투명 배경 PNG
9. JPEG 이미지
10. 지원하지 않는 파일
11. 문법이 손상된 JSON
12. 필수 항목이 빠진 JSON

12건 중 최소 한 건은 동일한 입력에 대해 다음 기록을 둘 다 남긴다.

* 수정 전 FAIL
* 결함 원인
* 수정 내용
* 수정 후 PASS

허위 결과를 작성하지 말고 실제 구현을 기준으로 검사한다.

## 8. 완성 이미지 3개와 권한 기록

서로 다른 문구 또는 화면비를 사용하는 완성 이미지 3개를 만들 수 있는 상태로 구현한다.

가능하면 프로젝트의 `public/samples` 또는 적절한 샘플 폴더에 직접 제작한 단순 배경 이미지 3개를 포함한다. 외부 이미지를 무단으로 가져오지 않는다.

`ASSET_LICENSES.md` 파일을 만들고 각 완성 이미지 또는 샘플 이미지에 대해 다음을 기록한다.

* 파일명
* 제작자 또는 출처
* 본인 제작 여부
* 외부 자료라면 원본 URL
* 라이선스명 또는 이용 허가 근거
* GPS/위치정보 메타데이터 제거 여부

직접 코드로 생성하거나 직접 만든 이미지라면 ‘본인 제작’ 또는 ‘프로젝트용 자체 생성’이라고 기록한다.

공개 이미지와 다운로드 이미지에 개인정보, API 키, 토큰, 비밀번호 등의 비밀값이 포함되지 않게 확인한다.

## 9. 짧은 확인 방법 및 AI 사용 기록

`SUBMISSION.md` 파일을 만들고 다음 내용을 명확히 구분해서 작성한다.

### 결과물 주소

배포 후 입력할 수 있도록 자리표시자를 둔다.

### 소스 주소

공개 저장소 주소를 입력할 수 있도록 자리표시자를 둔다.

### 짧은 확인 방법

반드시 다음 네 항목을 구분한다.

1. 어디로 가나요
2. 3단계 이내 무엇을 하나요
3. 무엇이 보이면 통과인가요
4. 안 될 때 무엇이 보이나요

각 기능별로 장황하게 쓰지 말고 실제 사용자가 3단계 이내에 확인할 수 있게 작성한다.

### AI 사용 기록

다음을 구분해서 작성한다.

1. AI에게 맡긴 일
2. 학생이 직접 판단한 일
3. AI 제안을 따르지 않은 일
4. 따르지 않은 일이 없다면 그 이유

‘전부 AI가 했다’거나 ‘전부 직접 했다’는 식으로 뭉뚱그리지 말고, 구현 보조와 학생의 설계·검증 판단을 구체적으로 구분한다.

## 10. 접근성 및 UI 품질

* 모든 폼 요소에 연결된 label을 제공한다.
* 키보드로 파일 선택, 값 변경, 템플릿 CRUD, 다운로드가 가능해야 한다.
* 현재 화면비와 선택된 템플릿 상태를 시각적으로 구분한다.
* 성공·오류 메시지는 색상만으로 구분하지 않고 문장으로 표시한다.
* 오류가 발생해도 전체 페이지가 깨지지 않게 한다.
* 모바일에서도 편집과 미리보기가 가능해야 한다.
* 과도한 애니메이션이나 기능과 무관한 장식은 넣지 않는다.

## 11. 반드시 확인할 통과 기준

구현 후 아래 항목을 하나씩 점검하고 `TEST_REPORT.md`에 대응 결과를 남긴다.

* T03-C03: 이미지 편집 도구와 문구 편집 도구가 첫 화면에 보임
* T03-C04: PNG 불러오기
* T03-C05: JPEG 불러오기
* T03-C06: 문구 위치 즉시 반영
* T03-C07: 문구 크기 즉시 반영
* T03-C08: 문구 색 즉시 반영
* T03-C09: 미지원 파일 뒤 기존 작업 유지
* T03-C10: 미지원 파일 거부 이유 표시
* T03-C11: 1:1 미리보기와 다운로드 일치
* T03-C12: 4:5 미리보기와 다운로드 일치
* T03-C13: 9:16 미리보기와 다운로드 일치
* T03-C14: 극단 입력 검사 12건
* T03-C15: 같은 입력의 수정 전 FAIL과 수정 후 PASS
* T03-C16: 잘못된 입력 뒤 기존 편집 보존
* T03-C17~C21: 템플릿 생성·불러오기·수정·삭제·새로고침 유지
* T03-C22: 정상 JSON 복원
* T03-C23: 손상 JSON 거부 및 기존 목록 유지
* T03-C24: 필수 항목 누락 JSON 거부 및 기존 목록 유지
* T03-C25~C27: 완성 이미지 3개와 권한 기록
* T03-C28: 공개 이미지 위치정보 메타데이터 0건
* T03-C29: 개인정보 0건
* T03-C30: 비밀값 원문 0건
* T03-C31: 짧은 확인 방법의 네 항목 구분
* T03-C32: AI 사용 기록의 세 항목 구분

T03-C01은 배포 후 새 시크릿 창에서 로그인 없이 열리는지 사용자가 최종 확인할 항목이므로, 앱 자체에는 로그인이나 인증 기능을 추가하지 않는다.

## 12. 구현 완료 후 수행할 작업

1. 타입 오류와 린트 오류를 확인한다.
2. 프로덕션 빌드를 실행한다.
3. 주요 기능을 브라우저에서 확인한다.
4. 발견한 오류를 수정한다.
5. 수정하거나 생성한 파일 목록을 보여준다.
6. 각 통과 기준이 코드의 어느 부분에서 충족되는지 간단히 정리한다.
7. 사용자가 직접 해야 하는 작업만 별도 목록으로 남긴다.
8. 실행 명령과 배포 방법을 알려준다.

작업 도중 사소한 선택을 사용자에게 계속 질문하지 말고 합리적인 기본값으로 진행해줘. 단, 기존 코드를 대규모로 삭제해야 하거나 프로젝트 구조가 요구사항과 충돌하는 경우에는 먼저 이유를 설명해줘.
