# TEST_REPORT — ZZALKAK Studio (짤칵 스튜디오)

이 문서의 모든 결과는 실제 구현을 기준으로 검사했다. 검사 방법은 두 가지다.

- **자동화 브라우저 검사**: `npm run dev`로 실행한 실제 앱을 헤드리스 Chromium(Playwright)으로 직접 조작해 캔버스 픽셀 데이터, 다운로드 파일 바이트, DOM 상태, `localStorage`, 콘솔 오류를 확인했다. (검사 스크립트는 검증 목적의 임시 파일이었으며 제출물에는 포함하지 않았다.)
- **수동 시각 확인**: 자동화 검사 중 캡처한 스크린샷을 직접 열어 실제 렌더링 결과를 눈으로 확인했다.

검사 환경: Windows 11, Chromium(Playwright headless), 데스크톱 1440×900 / 모바일 390×844 뷰포트, Vite dev server(localhost:5183).

아래 "업데이트 1~3"은 최초 제출 이후 추가된 기능을 새로 검사한 기록이며, 최신순(3→1)으로 배치했다. 그 뒤의 "1~4"는 최초 구현 시점의 기본 검사표다.

## 업데이트 3 (최신): T03-C03 회귀 수정 + 사진 없이 단색 배경으로 카드 만들기

### T03-C03 회귀: 문구 편집 도구가 첫 화면에서 사라짐

UI 개편 직후 CHECK.md 32개 기준을 다시 실측 점검한 결과, 첫 화면의 기본 도구가 "이미지"로 고정되어 있어 문구 편집 컨트롤(textarea·글꼴·크기·색상 등)이 클릭 전에는 전혀 보이지 않는 회귀를 발견했다(`.block-item` 개수 0, 오른쪽 속성 패널도 안내 문구만 표시). `src/editor/useEditor.ts`의 기본 도구를 `"image"`→`"text"`로, 기본 선택 대상을 `{kind:"none"}`→`{kind:"text", key:"body"}`로 바꿔, 첫 화면에서 클릭 없이 이미지 업로드 영역(중앙 캔버스)과 문구 편집 컨트롤(오른쪽 속성 패널: 문구·글꼴·크기·색상·정렬·줄간격·위치)이 동시에 보이도록 수정했다. 수정 후 스크린샷으로 두 도구가 동시에 보임을 확인했다(**수정 전 FAIL → 수정 후 PASS**).

### 사진 없이 단색 배경으로 카드 만들기

기존에는 배경 이미지를 올리지 않으면 캔버스 자체가 그려지지 않아 카드를 전혀 만들 수 없었다. `CardSettings.background: { type: "image" | "color"; color }`를 추가해, 왼쪽 "이미지" 도구(또는 캔버스 빈 상태의 "단색 배경으로 시작" 버튼, 도구 검색의 "단색 배경으로 전환")에서 단색 모드로 바꾸면 사진 없이도 문구·스티커만으로 카드를 완성하고 다운로드할 수 있게 했다. `src/lib/render.ts`의 `drawCard()`는 `background.type === "color"`이면 이미지 인자를 무시하고 지정한 색으로 캔버스를 채운다(미리보기·다운로드가 여전히 같은 함수 하나만 사용). 템플릿·JSON 스키마를 v4로 올려 배경 설정을 포함하고, v1~v3 템플릿은 자동으로 `{type:"image"}`로 이관된다.

헤드리스 Chromium으로 실제 앱을 조작해 다음을 확인했다(모두 PASS, 콘솔 오류 0건):

| 검사 내용 | 결과 |
|---|---|
| 캔버스 빈 상태에 "단색 배경으로 시작" 버튼 표시 및 클릭 시 사진 없이 캔버스 렌더링 | PASS |
| 사진 없는 상태에서 PNG 다운로드 버튼 활성화 + 다운로드 파일이 미리보기와 바이트 단위 일치 | PASS |
| 이미지 패널의 배경 종류 토글(이미지/단색)에서 색상 변경 시 캔버스 즉시 반영 | PASS |
| 단색 모드에서 사진 업로드 시 배경 모드가 자동으로 "이미지"로 전환 | PASS |
| 이미지→단색→이미지 전환 시 이미 불러온 사진 정보가 메모리에 유지됨 | PASS |
| 단색 배경(색상 포함)을 템플릿으로 저장 → 다른 배경으로 바꾼 뒤 다시 불러오면 색상까지 정확히 복원 | PASS |
| 새로고침 후 단색 배경 초안(자동 저장)이 유지됨 | PASS |
| `background.type`이 알 수 없는 값("gradient")인 JSON 가져오기 거부 + 기존 템플릿 목록 유지, 오류 메시지에 어떤 필드인지 명시 | PASS |
| `background` 필드가 없는 예전 v3 템플릿 JSON을 "이미지 배경"으로 자동 이관해 정상 가져오기 | PASS |

### 실제로 발견하고 고친 결함: 단색 배경에서도 스티커 도구가 막혀 있었음

- **증상**: 배경을 단색으로 바꾸면 캔버스는 정상적으로 그려지는데도, 스티커 패널의 모든 썸네일 버튼이 `disabled` 상태로 남아 클릭이 되지 않았다(자동화 테스트에서 `element is not enabled`로 실패 재현).
- **원인**: `src/components/panels/StickerPanel.tsx`의 스티커 추가 버튼이 여전히 `disabled={!editor.image}`로, 사진이 실제로 로드되어 있는지만 검사하고 단색 배경 모드는 고려하지 않았다.
- **수정**: `disabled={!canPlaceSticker}`로 바꾸고 `canPlaceSticker = background.type === "color" || Boolean(image)`로 계산해, 단색 배경에서도 스티커를 추가할 수 있게 했다. 안내 문구도 "먼저 이미지를 불러오거나 단색 배경을 선택해 주세요"로 갱신했다.
- **검증**: 동일한 조작(단색 배경 상태에서 스티커 썸네일 클릭)을 수정 전/후로 재현해, 수정 전 FAIL(버튼 비활성화로 클릭 불가) → 수정 후 PASS(정상 추가·드래그·삭제 가능)를 확인했다.

### 각 요소를 Backspace로도 삭제 가능하도록 보강

기존에는 스티커만 `Delete`/`Backspace` 키로 삭제할 수 있었다. 선택된 문구 블록(제목/소제목/본문)도 같은 키로 지울 수 있도록 `src/App.tsx`의 전역 키보드 핸들러를 확장했다(입력창에 포커스가 있을 때는 평소처럼 글자 삭제로 동작하고, 캔버스에서 블록만 "선택"된 상태일 때만 요소 삭제로 동작). 자동화 테스트로 문구 블록·스티커 각각 Backspace로 삭제되는 것을 확인했다(PASS).

## 업데이트 2: 편집기 UI 전면 개편 + 스티커 도구

카드형 세로 나열 화면을 **애플리케이션형 편집기**(상단 앱 바 / 왼쪽 도구 사이드바 + 세부 패널 / 중앙 캔버스 / 오른쪽 속성 패널, `100dvh`, 페이지 스크롤 없음)로 재설계했다. 기존 기능(이미지 업로드·검증, 제목/소제목/본문 편집, 화면비, 다운로드, 템플릿 CRUD, JSON 가져오기/내보내기)과 데이터 처리 로직은 그대로 유지했고, 다음을 추가했다: 실행 취소/다시 실행, 확대/축소, 도구 검색(Command Palette, `Ctrl/⌘+K`), 드래그 앤 드롭 업로드, 토스트 알림, 저장 상태 표시(초안 자동 저장), 정렬·줄 간격, **스티커 도구**(프로젝트 내부 PNG 16종, 검색·카테고리 필터, 드래그 이동·크기·회전·좌우 반전·복제·삭제·레이어 순서, 비율 좌표 저장, 템플릿/JSON 포함).

헤드리스 Chromium(Playwright)으로 실제 앱을 조작해 아래 37개 항목을 검사했고 모두 PASS, 콘솔 오류 0건이었다.

| 구분 | 검사 내용 | 결과 |
|---|---|---|
| 레이아웃 | 1440×900에서 `scrollHeight ≤ innerHeight`(페이지 스크롤 없음), 이미지/텍스트 도구가 첫 화면에 보임 | PASS |
| T03-C09/C10 | GIF 업로드 시 "PNG와 JPEG 형식의 이미지만 지원합니다" 토스트 + 빈 상태·기존 작업 유지 | PASS |
| T03-C04/C05 | PNG 업로드, JPEG 드래그 앤 드롭 업로드(`drop` 이벤트) 후 캔버스 표시 | PASS |
| T03-C06/C07/C08 | 속성 패널에서 크기·색·위치 변경 시 캔버스 `toDataURL()` 즉시 변화 | PASS |
| 정렬 | 왼쪽 정렬 변경 시 캔버스 변화 | PASS |
| 스티커 검색/필터 | "하트" 검색 → 2개, "장식" 필터 → 5개 | PASS |
| 스티커 추가 | 썸네일 클릭 → 캔버스 중앙(50%,50%)에 추가·선택·가이드 표시 | PASS |
| 스티커 드래그 | 캔버스에서 (50,50)→(30,40)% 드래그 이동 | PASS |
| 스티커 크기/회전 | 모서리 손잡이 드래그 28%→43%, 회전 손잡이 드래그 0°→90° | PASS |
| 스티커 반전/복제/레이어/삭제 | 좌우 반전 시 픽셀 변화, 복제로 2개, "맨 뒤"로 레이어 1/2, `Delete` 키로 삭제 | PASS |
| 실행 취소/다시 실행 | `Ctrl+Z`로 삭제 취소(2개), `Ctrl+Shift+Z`로 재적용(1개) | PASS |
| T03-C11/C12/C13 | 스티커+3개 글꼴 상태에서 1:1/4:5/9:16 미리보기 `toDataURL`과 다운로드 PNG 바이트 일치 | PASS |
| T03-C17~C21 | 템플릿 3개 생성, 이름 변경, 삭제, 새로고침 후 유지 + 초안(스티커 포함) 복원, 불러오기 시 제목/글꼴/스티커 복원 | PASS |
| JSON 내보내기 | `version: 3`, 각 템플릿에 `stickers` 배열 포함 | PASS |
| T03-C22 | 같은 ID JSON 가져오기 → 기존 것 유지, 새 이름으로 추가(개수 +2) | PASS |
| T03-C23 | 문법 손상 JSON 거부, 목록 불변 | PASS |
| T03-C24 | 프로젝트에 없는 `stickerId` JSON 거부(어떤 필드인지 명시), 목록 불변 | PASS |
| 호환성 | 예전 v2 JSON(스티커·정렬 없음) 자동 이관 가져오기 | PASS |
| 도구 검색 | `Ctrl+K` → "9:16" → Enter로 화면비 변경, "소제목 편집" 실행 시 속성 입력 포커스, Esc로 닫힘 | PASS |
| 캔버스 선택 | 문구 묶음 드래그(50,82)→(40,60)%, 빈 곳 클릭 시 이미지 선택, Esc 선택 해제, Tab 키보드 이동 | PASS |
| 모바일 | 390×844에서 페이지 스크롤 없음, 하단 탭 + 바텀시트 1개만 표시, 캔버스가 시트 위에 보임 | PASS |

수정한 결함(실측): 모바일에서 화면을 줄일 때 도구 시트와 속성 시트가 동시에 열려 캔버스를 가리는 문제 → 한 번에 하나만 표시하도록 수정. 바텀시트가 DOM 순서상 캔버스 위에 배치되던 문제 → `order`로 하단 배치. 토스트가 확대/축소 바를 가리던 문제 → 상단 바 아래로 이동.

## 업데이트 1: 제목/소제목/본문 분리 + 글꼴 선택

기존 단일 "문구" 입력을 **제목/소제목/본문 3개의 독립적인 입력**으로 나누고, 각각 원하는 것만 선택적으로 입력할 수 있게 했다(모두 비워도 되고, 하나만 채워도 되고, 전부 채워도 된다). 각 블록은 서로 다른 **글꼴·글자 크기·색상**을 가질 수 있으며, 위치(X/Y)는 세 블록을 하나의 묶음으로 취급해 함께 이동한다. 기본 글꼴은 Pretendard이며, 총 11종(Pretendard, 그리운연우, 갈무리11, 네오둥근모, 원스토어 모바일 고딕 타이틀, Jua, Do Hyeon, 나눔손글씨 펜, Poor Story, 감자꽃, Mona12 Text KR)을 제공한다. 관련 코드: `src/lib/fonts.ts`(글꼴 목록), `src/fonts.css`(`@font-face`/`@import`), `src/lib/fontLoader.ts`(`document.fonts.ready` 대기), `src/lib/render.ts`의 `drawCard`(3블록을 세로로 쌓아 배치하고, 전체 높이가 넘치면 비율을 유지한 채 자동 축소).

### 실제로 발견하고 고친 결함: 특정 글꼴에서 한글 글자 외곽선이 깨져 보임

- **증상**: 제목에 "Black Han Sans" 글꼴을 선택하고 "짤칵 스튜디오"처럼 쌍자음이 들어간 글자("짤")를 입력하면, 미리보기와 **다운로드된 PNG 파일 모두에서** 해당 글자의 획이 겹쳐 보이는 렌더링 결함이 5회 재현 중 5회 모두 나타났다(`page.setInputFiles`로 이미지 업로드 → 제목 문구 입력 → 글꼴을 Black Han Sans로 변경하는 실제 사용자 흐름을 그대로 재현해 확인).
- **조사**: 단순히 같은 글꼴·크기로 텍스트만 그리는 격리된 재현 코드에서는 문제가 재현되지 않아, 웹폰트 로딩 타이밍만의 문제가 아니라 이 글꼴 자체가 앱의 실제 렌더링 경로(이미지 cover 배치 → 자동 축소 계산 → 그림자 효과가 있는 텍스트 그리기)와 결합했을 때만 드러나는, 재현 조건이 좁은 렌더링 결함임을 확인했다. 다음 프레임에 한 번 더 그려보는 자가 복구 로직을 추가해도 고쳐지지 않아, 특정 글꼴 자체의 문제로 결론지었다.
- **조치**: 실제 사용자에게 깨진 결과물이 노출될 위험을 피하기 위해 해당 글꼴을 제공 목록에서 제외하고, 비슷한 용도의 임팩트 있는 제목용 글꼴인 **Jua**로 교체했다. 교체한 뒤 동일한 재현 절차를 5회 반복해 모두 정상 렌더링됨을 확인했고, 나머지 10종 글꼴 전체에 대해서도 쌍자음·받침이 있는 스트레스 문구("짤칵 스튜디오 값짧삶 쨍쫑 훑었다")로 같은 절차를 반복해 이상이 없음을 확인했다(나눔손글씨 펜에서 매우 희귀한 받침 글자 "훑"이 폰트 자체의 글리프 부족으로 시스템 폴백 글꼴로 표시되는 것은 정상적인 폴백 동작이며 결함이 아니다).
- **검증**: 위 "결함 발견 전(Black Han Sans) → FAIL", "결함 수정 후(Jua로 교체) → PASS"는 동일한 절차·입력을 5회씩 반복 재현한 결과다.

## 1. 극단 입력 12건 검사표

| 검사 ID | 검사 입력 | 예상 결과 | 실제 결과 | PASS/FAIL | 확인 방법 | 관련 통과 기준 |
|---|---|---|---|---|---|---|
| EDGE-01 | 공백 없이 이어지는 긴 한글 문장(약 65자, "가나다라마바사아자차카타파하..." 반복 + 문장) | 지정 폭을 넘지 않게 글자 단위로 자동 줄바꿈되어 미리보기에 잘림 없이 표시 | 5줄로 자동 줄바꿈되어 이미지 안에 모두 표시됨. 콘솔 오류 없음 | PASS | 텍스트 입력 후 스크린샷으로 줄바꿈 위치·잘림 여부 확인 | T03-C06, T03-C14 |
| EDGE-02 | 한글·영문·숫자·기호 혼합 ("2026년 ZZALKAK Studio 테스트 No.12 완료!") | 문자 종류에 관계없이 한 줄(또는 필요 시 자동 줄바꿈)로 정상 표시 | 정상 표시, 잘림·깨짐 없음 | PASS | 스크린샷 확인 + 콘솔 오류 0건 | T03-C14 |
| EDGE-03 | 사용자가 직접 입력한 여러 줄 문구(줄바꿈 3회) 및 큰 글자 크기(150px)+5줄 조합 | 사용자가 입력한 줄바꿈 위치가 그대로 유지되고, 글자 크기가 커도 이미지 밖으로 잘리지 않아야 함 | **1차 시도(수정 전) FAIL** → **수정 후 PASS**. 아래 "대표 결함" 참조 | PASS (수정 후) | 스크린샷으로 상하단 잘림 여부 확인 | T03-C06, T03-C07, T03-C14, **T03-C15** |
| EDGE-04 | 이모지 포함 문구 ("여름 세일 시작 🎉🔥😀 놓치지 마세요!") | 컬러 이모지가 깨지지 않고 한글과 함께 표시 | 이모지가 정상적으로 컬러로 렌더링되고 줄바꿈도 올바르게 적용됨 | PASS | 스크린샷으로 이모지 렌더링 확인 | T03-C14 |
| EDGE-05 | 빈 문구("") | 오류가 아니라 이미지에 글자가 표시되지 않는 정상 상태로 처리 | 오류 메시지 없이 글자만 사라지고 이미지는 그대로 유지됨 | PASS | 텍스트 삭제 후 콘솔 오류 0건, 이미지 유지 확인 | T03-C14 |
| EDGE-06 | 세로형 이미지 업로드 (900×1600 PNG, `bg-portrait.png`) | cover 방식으로 배치되어 세로 전체를 채움 | 정상 업로드 및 렌더링 | PASS | 파일명 표시 + 스크린샷 확인 | T03-C14 |
| EDGE-07 | 가로형 이미지 업로드 (1600×900 JPEG, `bg-landscape.jpg`) | cover 방식으로 배치되어 가로 전체를 채움 | 정상 업로드 및 렌더링 | PASS | 파일명 표시 + 스크린샷 확인 | T03-C14 |
| EDGE-08 | 투명 배경 PNG 업로드 (원형만 있고 나머지는 alpha=0) | 투명 영역이 있는 PNG도 오류 없이 업로드·렌더링 | 정상 업로드 및 렌더링, 콘솔 오류 없음 | PASS | 파일명 표시 + 스크린샷 확인 | T03-C14 |
| EDGE-09 | JPEG 이미지 업로드 (`bg-landscape.jpg`, `image/jpeg`) | MIME 타입 검사를 통과하고 정상 업로드 | 정상 업로드 (T03-C05와 동일 파일로 JPEG decode 경로 확인) | PASS | 파일명 표시 확인 | T03-C05, T03-C14 |
| EDGE-10 | 지원하지 않는 파일 업로드 (`.txt`, `text/plain`) | 파일을 불러오지 않고, 허용 형식(PNG/JPEG)을 포함한 거부 메시지를 표시하며 기존 이미지·문구를 유지 | 거부 메시지 "지원하지 않는 파일 형식입니다 (text/plain). PNG와 JPEG 형식의 이미지만 지원합니다." 표시. 업로드 직전 이미지(`transparent.png`)와 문구 값 그대로 유지됨 | PASS | 업로드 전후 파일명 텍스트, textarea 값 비교 | T03-C09, T03-C10, T03-C14 |
| EDGE-11 | 문법이 손상된 JSON 가져오기 (`{ "templates": [ { "name": "broken", "text": "test" ` — 중괄호/배열 미종료) | `JSON.parse` 단계에서 거부되고, 이해 가능한 오류 메시지 표시, 기존 템플릿 목록 변경 없음 | 오류 메시지 "JSON 문법이 올바르지 않아 가져오기를 중단했습니다. 기존 템플릿은 그대로 유지됩니다." 표시. 가져오기 전후 템플릿 개수 동일(4→4) | PASS | 가져오기 전후 템플릿 개수 비교 + 오류 메시지 확인 | T03-C23, T03-C14 |
| EDGE-12 | 필수 항목(`color`)이 빠진 JSON 가져오기 | 스키마 검증에서 거부되고 어떤 항목이 문제인지 표시, 기존 템플릿 목록 변경 없음, 일부만 저장되지 않음 | 오류 메시지 "1번째 항목의 "color" 값이 없거나 문자열이 아닙니다. 가져오기를 중단했으며 기존 템플릿은 그대로 유지됩니다." 표시. 개수 동일(4→4) | PASS | 가져오기 전후 템플릿 개수 비교 + 오류 메시지의 필드명 확인 | T03-C24, T03-C14 |

### 대표 결함: 수정 전 FAIL → 수정 후 PASS (EDGE-03)

- **검사 입력**: 사용자 입력 줄바꿈 5줄 + 글자 크기 150px, 1:1(1080×1080) 비율, 텍스트 Y 위치 50%.
- **수정 전 FAIL**: 문구 블록의 전체 높이(줄 수 × 줄간격)가 캔버스 높이를 초과했지만 이를 감지·보정하는 로직이 없어, 첫 줄과 마지막 줄이 캔버스 위/아래 경계 밖으로 그대로 잘려 나갔다. (`src/lib/render.ts`의 `drawCard`가 `settings.fontSize`를 그대로 사용해 `blockHeight`가 `canvas.height`를 넘어도 아무 보정을 하지 않았다.)
- **결함 원인**: 텍스트 줄바꿈은 가로 폭(`TEXT_MAX_WIDTH_RATIO`) 기준으로만 계산했고, 줄 수가 늘어나 세로로 넘치는 경우에 대한 처리가 전혀 없었다.
- **수정 내용**: `src/lib/render.ts`의 `drawCard`에 자동 축소(auto-fit) 로직을 추가했다. 요청한 글자 크기부터 시작해 줄바꿈을 다시 계산하면서, 문구 블록 전체 높이가 `TEXT_MAX_HEIGHT_RATIO`(캔버스 높이의 92%)를 넘으면 글자 크기를 2px씩 줄이고 다시 줄바꿈해, `FONT_SIZE_MIN`(16px)에 도달하거나 높이 조건을 만족할 때까지 반복한다. 미리보기와 다운로드가 이 함수 하나만 사용하므로 두 결과 모두 동일하게 보정된다. 사용자에게는 문구 편집 영역에 "문구가 길어 지정한 크기로 다 담을 수 없으면 잘리지 않도록 글자 크기가 자동으로 줄어듭니다" 안내 문구를 추가했다.
- **수정 후 PASS**: 동일한 입력(5줄 + 150px)으로 재검사한 결과, 글자 크기가 자동으로 축소되어 모든 줄이 캔버스 안에 완전히 표시됨을 스크린샷으로 확인했다.

## 2. 화면비별 미리보기/다운로드 일치 (카드 2)

같은 검사 문구("가장자리 확인용 문구입니다123ABC\n두번째 줄 - 줄바꿈 확인")로 1:1, 4:5, 9:16 각각에서 **미리보기 캔버스의 `toDataURL()` 결과와 실제 다운로드된 PNG 파일을 base64로 바이트 단위 비교**했다. 미리보기와 다운로드가 `src/lib/render.ts`의 `drawCard()` 하나만 공유하기 때문에 나오는 결과다.

| 통과 기준 | 화면비 | 미리보기 크기 | 다운로드 파일 크기 | 바이트 일치 | 결과 |
|---|---|---|---|---|---|
| T03-C11 | 1:1 (1080×1080) | 488,828B(base64) | 488,828B | 예 | PASS |
| T03-C12 | 4:5 (1080×1350) | 510,324B(base64) | 510,324B | 예 | PASS |
| T03-C13 | 9:16 (1080×1920) | 496,972B(base64) | 496,972B | 예 | PASS |

> 참고: 이 검사 과정에서 다운로드 버튼이 미리보기 캔버스 참조(`ref`)를 실제로 전달받지 못해 항상 "다운로드할 이미지가 없습니다" 오류를 내는 결함을 발견해 즉시 수정했다(`src/App.tsx`에서 `<PreviewCanvas>`에 `ref={canvasRef}`를 연결). 수정 전에는 이미지가 있어도 다운로드가 전혀 되지 않았고, 수정 후 위 표처럼 정상 동작을 확인했다.

### 2-1. 제목/소제목/본문 3블록 + 서로 다른 글꼴 조합 재검증

제목=짤칵 스튜디오(Jua), 소제목=제목 아래 소제목입니다(Do Hyeon), 본문=본문은 손글씨 폰트로\n두 줄까지 써봅니다(나눔손글씨 펜) 조합으로 동일한 바이트 비교를 다시 수행했다.

| 통과 기준 | 화면비 | 미리보기 크기 | 다운로드 파일 크기 | 바이트 일치 | 결과 |
|---|---|---|---|---|---|
| T03-C11 | 1:1 (1080×1080) | 487,528B(base64) | 487,528B | 예 | PASS |
| T03-C12 | 4:5 (1080×1350) | 508,900B(base64) | 508,900B | 예 | PASS |
| T03-C13 | 9:16 (1080×1920) | 496,728B(base64) | 496,728B | 예 | PASS |

다운로드 직전에는 `PreviewCanvasHandle.ensureFreshPaint()`가 웹폰트 로딩 상태를 다시 확인하고 같은 내용을 한 번 더 그려 캡처하므로, 여러 글꼴이 섞여 있어도 미리보기와 파일이 항상 같은 픽셀을 갖는다.

## 3. 나머지 통과 기준 점검 결과

| 기준 | 결과 | 확인 내용 |
|---|---|---|
| T03-C03 | PASS | 데스크톱 1440×900 뷰포트에서 스크롤 없이 이미지 업로드(`#image-file-input`)와 문구 편집(`#text-content`) 요소가 모두 `isVisible()` 함. |
| T03-C04 | PASS | `bg-square.png`(PNG) 업로드 후 "현재 이미지: bg-square.png" 표시 확인. |
| T03-C05 | PASS | `bg-landscape.jpg`(JPEG) 업로드 후 파일명 표시 확인. |
| T03-C06 | PASS | X/Y 슬라이더 변경 전후 캔버스 SHA-256 해시가 달라짐을 확인(즉시 반영). |
| T03-C07 | PASS | 글자 크기 슬라이더 변경 전후 캔버스 해시가 달라짐을 확인. |
| T03-C08 | PASS | 색상 변경 전후 캔버스 해시가 달라짐을 확인. |
| T03-C09 | PASS | GIF/TXT 업로드 시도 후에도 기존 이미지 파일명과 문구 값이 그대로 유지됨을 확인. |
| T03-C10 | PASS | 오류 메시지에 "PNG"와 "JPEG" 단어가 포함됨을 텍스트로 확인. |
| T03-C11~13 | PASS | 위 "2. 화면비별 미리보기/다운로드 일치" 표 참조. |
| T03-C14 | PASS | 위 12건 표 참조. |
| T03-C15 | PASS | EDGE-03 "대표 결함" 참조. |
| T03-C16 | PASS | EDGE-10에서 잘못된 파일 업로드 후 기존 이미지·문구가 예고 없이 사라지지 않음을 확인. |
| T03-C17 | PASS | 템플릿 3개("템플릿 A/B/C") 생성 후 목록에 3개 이상 표시됨. |
| T03-C18 | PASS | 문구를 임의로 바꾼 뒤 "템플릿 A" 불러오기 클릭 → 저장 당시 문구로 정확히 복원됨. |
| T03-C19 | PASS | "템플릿 B"를 "템플릿 B (수정됨)"으로 이름 변경 후 목록에 반영됨. |
| T03-C20 | PASS | "템플릿 C" 삭제 후 목록 개수가 1 감소하고 다른 템플릿은 영향 없음(안정적 UUID 기반 삭제). |
| T03-C21 | PASS | 페이지 새로고침(`page.reload`) 후에도 템플릿 개수와 변경된 이름이 그대로 유지됨(`localStorage` 확인). |
| T03-C22 | PASS | 정상 JSON(템플릿 2개) 가져오기 후 목록이 2개→4개로 증가하고 "2개 템플릿을 가져왔습니다" 메시지 표시. |
| T03-C23 | PASS | 문법 오류 JSON 가져오기 시 템플릿 개수 불변(4→4), 오류 메시지 표시. |
| T03-C24 | PASS | `color` 누락 JSON 가져오기 시 템플릿 개수 불변(4→4), 어떤 필드가 문제인지 메시지에 명시. |
| T03-C25~27 | PASS | `public/finished/`에 서로 다른 문구·화면비의 완성 이미지 3개 생성 및 확인. `ASSET_LICENSES.md`에 출처·허가 근거 기록. |
| T03-C28 | PASS | 모든 공개 이미지가 canvas 렌더링을 거쳐 생성되어 원본 EXIF/GPS 메타데이터가 결과 파일에 포함되지 않음(canvas 인코더는 메타데이터를 복사하지 않음). 샘플 이미지 자체도 canvas로 직접 생성해 GPS 정보가 애초에 존재하지 않음. |
| T03-C29 | PASS | 화면·코드·이미지 어디에도 이름·연락처 등 개인정보 없음. |
| T03-C30 | PASS | 코드·이미지·배포 파일 어디에도 API 키/토큰/비밀번호 원문 없음(서버 통신이 없는 순수 클라이언트 앱). |
| T03-C31 | PASS | `SUBMISSION.md`의 "짧은 확인 방법"에 4개 항목이 구분되어 있음. |
| T03-C32 | PASS | `SUBMISSION.md`의 "AI 사용 기록"에 3개 항목(+해당 없음 사유)이 구분되어 있음. |

## 4. 접근성/반응형 확인

- 모바일 뷰포트(390×844)에서 스크린샷 확인 결과 가로 스크롤 없이 1열로 재배치되고 모든 편집 요소가 정상 표시됨.
- 모든 입력 요소(`input`, `textarea`)는 `label`의 `htmlFor`로 연결되어 있어 키보드/스크린리더 접근이 가능함.
- 드래그로 문구 위치를 바꾸는 것 외에 X/Y 슬라이더로도 키보드(방향키/Tab)만으로 동일한 조작이 가능함을 확인.
- 삭제 등 파괴적 동작 전 `window.confirm`으로 확인 절차를 거침(다이얼로그 수락 시에만 삭제됨을 확인).
